import 'package:flutter/material.dart';

class GospelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gospel'),
      ),
      body: Center(
        child: Text('Gospel Page Content'),
      ),
    );
  }
}